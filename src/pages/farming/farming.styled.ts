import styled from "styled-components";

const FarmingContainer = styled.div`
  margin: 0 545px 0 575px;
  height: calc(100vh - 255px);
  display: grid;
  grid-template-columns: 1fr;
  gap: 35px;
  padding: 75px;
  overflow-y: auto;
`;

export { FarmingContainer };
